import type { Sprites } from "../SpriteLoader";

enum Difficulty {
  Easy = 0,
  Normal,
  Hard
}

class EntityLoader {
  constructor(sprites: Readonly<Sprites>,difficulty: Difficulty) {
    
  }
}

export default EntityLoader;
